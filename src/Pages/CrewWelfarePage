/* eslint-disable */
import { useState } from "react";

export default function CrewWelfarePage() {
  const [tab, setTab] = useState("finance");

  return (
    <div style={{ padding: "2rem", color: "var(--text)", flex: 1 }}>
      <h2 style={{ fontFamily: "'Orbitron',monospace", color: "var(--cyan)", marginBottom: "1rem" }}>
        CREW WELFARE
      </h2>
      <div style={{ display: "flex", gap: 10, marginBottom: "1.5rem" }}>
        {["finance", "planning", "wellbeing"].map(t => (
          <button key={t} onClick={() => setTab(t)} style={{
            padding: "8px 16px", borderRadius: 8, cursor: "pointer",
            background: tab === t ? "rgba(0,180,216,0.2)" : "var(--card)",
            border: "1px solid " + (tab === t ? "var(--cyan)" : "var(--border)"),
            color: tab === t ? "var(--cyan)" : "var(--text2)",
            fontFamily: "'Orbitron',monospace", fontSize: "0.72rem",
          }}>{t.toUpperCase()}</button>
        ))}
      </div>
      <div style={{ background: "var(--card)", border: "1px solid var(--border)", borderRadius: 12, padding: "1.5rem" }}>
        <p style={{ color: "var(--text2)", fontSize: "0.85rem" }}>
          Active tab: <strong style={{ color: "var(--cyan)" }}>{tab}</strong>
        </p>
        <p style={{ color: "var(--text3)", fontSize: "0.75rem", marginTop: 8 }}>
          Full tools coming soon. This confirms the page is loading correctly.
        </p>
      </div>
    </div>
  );
}
